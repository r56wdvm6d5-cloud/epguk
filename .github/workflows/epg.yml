name: EPG Workflow

on:
  push:
    branches:
      - main

jobs:
  process:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Process EPG Sources
        run: ./process_all_epg_sources.sh